"use client";
import { createTheme } from "@mui/material/styles";

const LinkButtonStyle = createTheme({
  typography: {
    subtitle2: {
      // Your styles here
      // For example:
      fontSize: "1rem",
      fontWeight: 500,
    },
    // Add other variants as needed
  },
});

export default LinkButtonStyle;
